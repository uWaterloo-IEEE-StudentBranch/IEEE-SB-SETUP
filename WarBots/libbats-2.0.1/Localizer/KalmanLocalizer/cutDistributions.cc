#include "kalmanlocalizer.ih"

/*
void KalmanLocalizer::cutDistributions()
{
  for (unsigned o = 0; o < Types::NOBJECTS; ++o)
  {
    d_locationsGlobal[o]->init(cutLocVec(d_locationsVelocitiesGlobal[o]->getMu()), cutLocMat(d_locationsVelocitiesGlobal[o]->getSigma()));
    d_velocitiesGlobal[o]->init(cutVelVec(d_locationsVelocitiesGlobal[o]->getMu()), cutVelMat(d_locationsVelocitiesGlobal[o]->getSigma()));
    d_locationsLocal[o]->init(cutLocVec(d_locationsVelocitiesLocal[o]->getMu()), cutLocMat(d_locationsVelocitiesLocal[o]->getSigma()));
    d_velocitiesLocal[o]->init(cutVelVec(d_locationsVelocitiesLocal[o]->getMu()), cutVelMat(d_locationsVelocitiesLocal[o]->getSigma()));
  }
}

*/
